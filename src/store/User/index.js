const user = {
  token: '',
  uid: '',
  permission: []
};

export default (state = user, action) => {
  let newState = { ...state };
  switch (action.type) {
  default:
    break;
  }
  return newState;
};